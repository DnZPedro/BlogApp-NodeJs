<div class="jumbotron">
    <h1 class="display-4">Bem vindo ao Blog com NodeJs!</h1>
    <p class="lead">Este é um blog simples, desenvolvido com Node.Js + Express + MongoDB</p>
    <hr class="my-4">
    <p>Aqui você encontra novidades sobre o mundo da computação e novas tecnologias!</p>
    <a class="btn btn-primary btn-lg" href="#" role="button">Crie Uma Conta</a>
</div>

<hr>

<h2>Postagens Recentes</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia Mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Data de Publicação: {{data}}</small>
    </div>
</div>
{{else}}
<h5>Nenhuma Postagem</h5>
{{/each}}